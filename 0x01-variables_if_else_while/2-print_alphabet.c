#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: return 0 (success)
 */
int main(void)
{
char ch = 'a';
putchar("Lower case alphabet");
while (ch <= z)
{
putchar("%c", ch);
ch++;
}
putchar("\n");

return (0);
}
